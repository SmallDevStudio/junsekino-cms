import ProjectManager from "@/components/admin/project/ProjectManager";

export const metadata = {
  title: "Projects | Junsekino CMS",
};

export default function AdminProjectsPage() {
  return <ProjectManager />;
}
