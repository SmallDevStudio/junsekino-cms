import MessageManager from "@/components/admin/message/MessageManager";

export const metadata = {
  title: "Messages | Junsekino CMS",
};

export default function AdminMessagesPage() {
  return <MessageManager />;
}
